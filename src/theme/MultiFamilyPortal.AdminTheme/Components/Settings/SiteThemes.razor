<TelerikGrid Data=@_themes>
  <GridColumns>
    <GridColumn Field=@nameof(Data.Models.SiteTheme.Id) />
    <GridColumn Field=@nameof(Data.Models.SiteTheme.IsDefault)>
      <Template>
        @{ var template = context as Data.Models.SiteTheme; }
        @if(template.IsDefault)
        {
          <span>Default</span>
        }
      </Template>
    </GridColumn>
    <GridCommandColumn>
      @{ var template = context as Data.Models.SiteTheme; }
      @if(!template.IsDefault)
      {
        <GridCommandButton Command="SetDefault" Title="Set Default" OnClick=@SetDefault>Set Default</GridCommandButton>
      }
    </GridCommandColumn>
  </GridColumns>
</TelerikGrid>

<PortalNotification @ref="notification" />
