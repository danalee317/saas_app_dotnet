<TelerikGrid Data=@_logos
             @ref="grid">
  <GridColumns>
    <GridColumn Field="DisplayName" />
    <GridColumn Title="Logo" TextAlign=@ColumnTextAlign.Center>
      <Template>
        @{ var logo = context as Logo; }
        <img src="@($"{logo.Href}id={DateTime.Now.Ticks}")" alt="@logo.DisplayName" class="img img-fluid my-3" style="max-width:250px;max-height:125px;" />
      </Template>
    </GridColumn>
    <GridColumn Field="Size" TextAlign=@ColumnTextAlign.Center />
    <GridCommandColumn>
       <div class="text-center">
          <GridCommandButton Command="ReplaceLogo"
                              Title="Update Logo"
                              Primary="true"
                              IconClass="fa-duotone fa-upload"
                              OnClick=@UpdateLogo>Update Logo
               </GridCommandButton>
         </div>
     </GridCommandColumn>
   </GridColumns>
</TelerikGrid>
<TelerikWindow Visible=@showWindow Modal=@true>
  <WindowTitle>Update @_selected.DisplayName</WindowTitle>
  <WindowActions>
    <WindowAction Name="Close" OnClick=@(() => showWindow = false) />
  </WindowActions>
  <WindowContent>
    <p>
      You may upload a JPEG, or PNG. We recommend png for now. We are working to support SVG as well.
    </p>
    <TelerikUpload AllowedExtensions="@AllowedFileTypes"
                    MaxFileSize="2048000"
                    SaveField="file"
                    Multiple="false"
                    OnSuccess="@OnSuccessHandler"
                    SaveUrl=@LogoUrl(_selected.Name) />
  </WindowContent>
</TelerikWindow>
